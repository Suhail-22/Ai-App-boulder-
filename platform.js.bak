// platform.js - الإصدار المطور
import AIService from './services/ai-service.js';
import CONFIG from './config.js';

class AIDevPlatform {
    constructor() {
        // إعدادات المستخدم
        this.settings = {
            theme: 'light',
            language: 'ar',
            aiProvider: 'GEMINI',
            autoSave: true,
            fontSize: 14
        };
        
        // المشروع الحالي
        this.currentProject = this.createNewProject();
        
        // المحرر
        this.editor = null;
        this.currentFile = null;
        
        // الذكاء الاصطناعي
        this.aiService = new AIService(this.settings.aiProvider);
        
        // السجلات
        this.terminalHistory = [];
        this.chatHistory = [];
        this.codeHistory = [];
        
        // التهيئة
        this.init();
    }
    
    createNewProject(name = 'مشروع جديد') {
        return {
            id: 'project-' + Date.now(),
            name: name,
            description: 'مشروع تم إنشاؤه في Bolt AI',
            files: [
                {
                    id: 'file-1',
                    name: 'index.html',
                    path: '/index.html',
                    content: this.getDefaultHTML(),
                    language: 'html',
                    isOpen: true
                },
                {
                    id: 'file-2',
                    name: 'style.css',
                    path: '/style.css',
                    content: this.getDefaultCSS(),
                    language: 'css',
                    isOpen: false
                },
                {
                    id: 'file-3',
                    name: 'app.js',
                    path: '/app.js',
                    content: this.getDefaultJS(),
                    language: 'javascript',
                    isOpen: false
                }
            ],
            libraries: [],
            createdAt: new Date(),
            updatedAt: new Date()
        };
    }
    
    getDefaultHTML() {
        return `<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀 مشروع Bolt AI</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="logo">
                <i class="fas fa-bolt"></i>
                <h1>Bolt AI Studio</h1>
            </div>
            <p class="subtitle">منصة التطوير بالذكاء الاصطناعي</p>
        </header>
        
        <main class="main-content">
            <div class="hero">
                <h2>مرحباً بك في عالم البرمجة الذكي! 👋</h2>
                <p>هذا مشروعك الأول الذي تم إنشاؤه بواسطة منصة Bolt AI. يمكنك تعديله كما تريد!</p>
                
                <div class="actions">
                    <button class="btn btn-primary" id="actionBtn">
                        <i class="fas fa-magic"></i> جرب التفاعل
                    </button>
                    <button class="btn btn-outline" id="themeToggle">
                        <i class="fas fa-moon"></i> تبديل السمة
                    </button>
                </div>
                
                <div class="features">
                    <div class="feature-card">
                        <i class="fas fa-code"></i>
                        <h3>كتابة ذكية</h3>
                        <p>اكتب الكود باستخدام الذكاء الاصطناعي</p>
                    </div>
                    <div class="feature-card">
                        <i class="fas fa-eye"></i>
                        <h3>معاينة حية</h3>
                        <p>شاهد التغييرات فوراً</p>
                    </div>
                    <div class="feature-card">
                        <i class="fas fa-rocket"></i>
                        <h3>نشر سهل</h3>
                        <p>انشر مشروعك بنقرة واحدة</p>
                    </div>
                </div>
            </div>
        </main>
        
        <footer class="footer">
            <p>تم الإنشاء بواسطة <strong>Bolt AI Platform</strong> | ${new Date().getFullYear()}</p>
        </footer>
    </div>
    
    <script src="app.js"></script>
</body>
</html>`;
    }
    
    getDefaultCSS() {
        return `/* Bolt AI - التنسيقات الافتراضية */

:root {
    --primary-color: #4f46e5;
    --primary-dark: #4338ca;
    --secondary-color: #10b981;
    --secondary-dark: #059669;
    --text-primary: #1e293b;
    --text-secondary: #64748b;
    --bg-light: #f8fafc;
    --bg-white: #ffffff;
    --border-color: #e2e8f0;
    --shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    --radius: 12px;
    --transition: all 0.3s ease;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', 'Inter', system-ui, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: var(--text-primary);
    min-height: 100vh;
    line-height: 1.6;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
}

/* الهيدر */
.header {
    text-align: center;
    margin-bottom: 3rem;
}

.logo {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 1rem;
}

.logo i {
    font-size: 2.5rem;
    color: var(--primary-color);
    animation: pulse 2s infinite;
}

.logo h1 {
    font-size: 2.5rem;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.subtitle {
    font-size: 1.1rem;
    color: var(--text-secondary);
}

/* المحتوى الرئيسي */
.main-content {
    background: var(--bg-white);
    border-radius: var(--radius);
    padding: 3rem;
    box-shadow: var(--shadow);
    margin-bottom: 2rem;
}

.hero {
    text-align: center;
}

.hero h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: var(--primary-dark);
}

.hero p {
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

/* الأزرار */
.actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 3rem;
    flex-wrap: wrap;
}

.btn {
    padding: 12px 24px;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.btn-primary {
    background: var(--primary-color);
    color: white;
}

.btn-primary:hover {
    background: var(--primary-dark);
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3);
}

.btn-outline {
    background: transparent;
    border: 2px solid var(--border-color);
    color: var(--text-primary);
}

.btn-outline:hover {
    border-color: var(--primary-color);
    color: var(--primary-color);
}

/* بطاقات الميزات */
.features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
}

.feature-card {
    background: var(--bg-light);
    padding: 2rem;
    border-radius: var(--radius);
    text-align: center;
    transition: var(--transition);
}

.feature-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow);
}

.feature-card i {
    font-size: 2.5rem;
    color: var(--primary-color);
    margin-bottom: 1rem;
}

.feature-card h3 {
    margin-bottom: 0.5rem;
    color: var(--primary-dark);
}

.feature-card p {
    color: var(--text-secondary);
    font-size: 0.95rem;
}

/* الفوتر */
.footer {
    text-align: center;
    color: white;
    padding: 1.5rem;
    background: rgba(0, 0, 0, 0.2);
    border-radius: var(--radius);
}

/* الأنيميشن */
@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}

/* التصميم المتجاوب */
@media (max-width: 768px) {
    .container {
        padding: 1rem;
    }
    
    .main-content {
        padding: 2rem 1.5rem;
    }
    
    .logo h1 {
        font-size: 2rem;
    }
    
    .hero h2 {
        font-size: 1.5rem;
    }
    
    .features {
        grid-template-columns: 1fr;
    }
}`;
    }
    
    getDefaultJS() {
        return `// Bolt AI - JavaScript الافتراضي

class BoltApp {
    constructor() {
        this.init();
        this.bindEvents();
        this.loadPreferences();
    }
    
    init() {
        console.log('🚀 Bolt AI Application Started!');
        
        // عناصر DOM
        this.elements = {
            actionBtn: document.getElementById('actionBtn'),
            themeToggle: document.getElementById('themeToggle'),
            body: document.body
        };
        
        // الحالة
        this.state = {
            theme: 'light',
            clickCount: 0,
            isAnimating: false
        };
    }
    
    bindEvents() {
        // زر الإجراء الرئيسي
        if (this.elements.actionBtn) {
            this.elements.actionBtn.addEventListener('click', (e) => {
                this.handleActionClick(e);
            });
        }
        
        // تبديل السمة
        if (this.elements.themeToggle) {
            this.elements.themeToggle.addEventListener('click', (e) => {
                this.toggleTheme();
            });
        }
        
        // تحميل الصفحة
        window.addEventListener('load', () => this.onPageLoad());
        
        // قبل إغلاق الصفحة
        window.addEventListener('beforeunload', () => this.savePreferences());
    }
    
    handleActionClick(event) {
        this.state.clickCount++;
        
        // تأثير على الزر
        const button = event.currentTarget;
        this.animateButton(button);
        
        // إشعار
        this.showNotification(\`✨ تم النقر \${this.state.clickCount} مرة!\`);
        
        // تحديث الزر
        if (this.state.clickCount === 1) {
            button.innerHTML = '<i class="fas fa-star"></i> ممتاز! جرب مرة أخرى';
        } else if (this.state.clickCount === 3) {
            button.innerHTML = '<i class="fas fa-fire"></i> 🔥 أنت رائع!';
        } else if (this.state.clickCount === 5) {
            button.innerHTML = '<i class="fas fa-trophy"></i> 🏆 مبروك! وصلت لـ 5 نقرات!';
        }
        
        // تغيير لون عشوائي بعد 3 نقرات
        if (this.state.clickCount >= 3) {
            this.changeRandomColor(button);
        }
        
        // تسجيل في الكونسول
        console.log(\`✅ النقرة رقم: \${this.state.clickCount}\`);
    }
    
    animateButton(button) {
        if (this.state.isAnimating) return;
        
        this.state.isAnimating = true;
        
        // تأثير النبض
        button.style.transform = 'scale(0.95)';
        button.style.boxShadow = '0 5px 15px rgba(79, 70, 229, 0.4)';
        
        setTimeout(() => {
            button.style.transform = 'scale(1)';
            button.style.boxShadow = '';
            this.state.isAnimating = false;
        }, 150);
    }
    
    changeRandomColor(element) {
        const colors = [
            '#4f46e5', '#10b981', '#f59e0b', '#ef4444', '#8b5cf6', '#ec4899'
        ];
        
        const randomColor = colors[Math.floor(Math.random() * colors.length)];
        element.style.backgroundColor = randomColor;
        
        // العودة للون الأصلي بعد ثانية
        setTimeout(() => {
            element.style.backgroundColor = '';
        }, 1000);
    }
    
    toggleTheme() {
        this.state.theme = this.state.theme === 'light' ? 'dark' : 'light';
        
        // تطبيق السمة
        this.elements.body.setAttribute('data-theme', this.state.theme);
        
        // تحديث الزر
        const icon = this.state.theme === 'dark' ? 'fa-sun' : 'fa-moon';
        this.elements.themeToggle.innerHTML = \`<i class="fas \${icon}"></i> \${this.state.theme === 'dark' ? 'الوضع الفاتح' : 'الوضع الداكن'}\`;
        
        // إشعار
        this.showNotification(\`🌙 تم التبديل إلى الوضع \${this.state.theme === 'dark' ? 'الداكن' : 'الفاتح'}\`);
        
        // حفظ التفضيل
        localStorage.setItem('bolt-theme', this.state.theme);
    }
    
    showNotification(message) {
        // إنشاء عنصر الإشعار
        const notification = document.createElement('div');
        notification.className = 'bolt-notification';
        notification.innerHTML = \`
            <div class="notification-content">
                <i class="fas fa-check-circle"></i>
                <span>\${message}</span>
            </div>
        \`;
        
        // التنسيق
        notification.style.cssText = \`
            position: fixed;
            top: 20px;
            left: 20px;
            background: var(--primary-color);
            color: white;
            padding: 12px 20px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            z-index: 1000;
            animation: slideIn 0.3s ease;
        \`;
        
        // إضافة للصفحة
        document.body.appendChild(notification);
        
        // إزالتها بعد 3 ثواني
        setTimeout(() => {
            notification.style.animation = 'slideOut 0.3s ease';
            setTimeout(() => notification.remove(), 300);
        }, 3000);
    }
    
    onPageLoad() {
        console.log('📄 الصفحة تم تحميلها بنجاح');
        this.showNotification('🚀 مشروع Bolt AI جاهز للعمل!');
        
        // تحميل السمة المحفوظة
        const savedTheme = localStorage.getItem('bolt-theme');
        if (savedTheme) {
            this.state.theme = savedTheme;
            this.elements.body.setAttribute('data-theme', savedTheme);
            
            const icon = savedTheme === 'dark' ? 'fa-sun' : 'fa-moon';
            if (this.elements.themeToggle) {
                this.elements.themeToggle.innerHTML = \`<i class="fas \${icon}"></i> \${savedTheme === 'dark' ? 'الوضع الفاتح' : 'الوضع الداكن'}\`;
            }
        }
    }
    
    loadPreferences() {
        // يمكنك إضافة تحميل التفضيلات هنا
        console.log('⚙️ تحميل التفضيلات...');
    }
    
    savePreferences() {
        // حفظ التفضيلات قبل المغادرة
        localStorage.setItem('bolt-click-count', this.state.clickCount.toString());
        console.log('💾 تم حفظ التفضيلات');
    }
}

// بدء التطبيق
document.addEventListener('DOMContentLoaded', () => {
    window.boltApp = new BoltApp();
});

// أنيميشن CSS للإشعارات
const style = document.createElement('style');
style.textContent = \`
    @keyframes slideIn {
        from {
            transform: translateX(-100%);
            opacity: 0;
        }
        to {
            transform: translateX(0);
            opacity: 1;
        }
    }
    
    @keyframes slideOut {
        from {
            transform: translateX(0);
            opacity: 1;
        }
        to {
            transform: translateX(-100%);
            opacity: 0;
        }
    }
    
    [data-theme="dark"] {
        --text-primary: #f1f5f9;
        --text-secondary: #cbd5e1;
        --bg-light: #1e293b;
        --bg-white: #0f172a;
        --border-color: #334155;
    }
\`;
document.head.appendChild(style);`;
    }
    
    init() {
        this.loadUserSettings();
        this.initUI();
        this.initEditor();
        this.setupEventListeners();
        this.updatePreview();
        this.setupAI();
    }
    
    loadUserSettings() {
        const savedSettings = localStorage.getItem('bolt-settings');
        if (savedSettings) {
            this.settings = { ...this.settings, ...JSON.parse(savedSettings) };
        }
        
        // تطبيق الإعدادات
        document.documentElement.lang = this.settings.language;
        document.documentElement.dir = this.settings.language === 'ar' ? 'rtl' : 'ltr';
        
        if (this.settings.theme === 'dark') {
            document.body.classList.add('dark-mode');
        } else {
            document.body.classList.add('light-mode');
        }
    }
    
    initUI() {
        this.updateFileTree();
        this.updateEditorTabs();
        this.updateThemeToggle();
        this.updateLanguageToggle();
        this.loadTemplates();
        this.loadAIProviders();
    }
    
    async setupAI() {
        // محاولة الاتصال بالذكاء الاصطناعي
        const apiKey = localStorage.getItem('bolt-ai-key');
        if (apiKey) {
            const connected = await this.aiService.connect(apiKey);
            if (connected) {
                this.showAIMessage('✅ متصل بخدمة الذكاء الاصطناعي بنجاح');
            }
        }
    }
    
    // ... (بقية الدوال)
}